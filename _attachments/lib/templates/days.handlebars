<ul class="unstyled" >
	{{#each days}}
		<li>
			<a href="#" {{action "loadDate" target="IRC" }} ><strong>{{format date "%Y-%m-%d"}}</strong></a> ({{count}})
		</li>
		{{else}}
		loading...
	{{/each}}
</ul>